func main[] : i64
    let sum = 0
    let a = 0
    let b = 1

    while a < 4000000
        if a % 2 == 0
            sum = sum + a
        let temp: i64 = b
        b = a + b
        a = temp

    io.println_i64(sum)