func main[] : I64
    let found: I64 = 0

    let i: I64 = 1
    while true
        if math.is_prime(i)
            found = found + 1
            if found == 10001
                io.println_i64(i)
                break
        i = i + 1