func print_i64[x: I64] : I64
    printf("%ld\n", x)

func main[] : I64
    let sum: I64 = 0

    let i: I64 = 0
    while i < 1000
        if i % 5 == 0 || i % 3 == 0
            sum = sum + i
        i = i + 1
    print_i64(sum)