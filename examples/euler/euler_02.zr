include "std/io.zr"

func main[] : i64
    sum := 0
    a := 0
    b := 1

    while a < 4000000
        if a % 2 == 0
            sum += a
        temp := b
        b += a
        a = temp

    io.println_i64(sum)
