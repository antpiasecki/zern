func main[] : I64
    let arr: Array = []
    for i in 0..10
        array.push(arr, math.abs(math.urandom() % 1000))

    for i in 0..array.size(arr)
        io.print_i64(array.nth(arr, i))
    io.print("------------")

    alg.quicksort(arr)

    for i in 0..array.size(arr)
        io.print_i64(array.nth(arr, i))

    array.free(arr)